#!/bin/sh /etc/rc.common
# Copyright (C) 2006-2011 OpenWrt.org

START=96

start() {
	SERVICE_DAEMONIZE=1 service_start /usr/sbin/logserver start
}

stop() {
	 ps | grep -E "logserver/main|/logserver" | grep -v grep  | awk '{print $1}' | xargs kill
}
