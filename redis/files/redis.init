#!/bin/sh /etc/rc.common
# Copyright (C) 2006-2011 OpenWrt.org

START=60
PROGRAM=redis-server
start() {
	SERVICE_DAEMONIZE=1 service_start /usr/sbin/redis
}

stop() {
	 ps | grep -E "$PROGRAM" | grep -v grep  | awk '{print $1}' | xargs kill
}
